import { StyleSheet } from "react-native";
import { black, pink } from "./variables";

export const globalStyle = StyleSheet.create({
    pinkText: {
        color: pink,
    },
    blackText: {
        color: black,
    }
})