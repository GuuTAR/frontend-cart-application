import { StyleSheet } from "react-native";

export const stylesheet = StyleSheet.create({
    cartPage: {
        flex: 1
    },
    productContainer: {
        width: '100%',
        alignItems: 'center',
    }
})