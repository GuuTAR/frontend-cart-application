import { colorStyle, layoutStyle } from "../../style/globalStyle"
import { stylesheet } from "./sheet"

export const previewImgContainer = [stylesheet.previewImgContainer, colorStyle.blackBG, layoutStyle.center]

export const image = [stylesheet.image]

export const buttonWrapper = [stylesheet.buttonWrapper]