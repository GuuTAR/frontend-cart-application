import { colorStyle, textStyle } from "../../style/globalStyle";
import { stylesheet } from "./sheet";

export const homePage = [stylesheet.homePage, colorStyle.grayBG]

export const searchContainer = [stylesheet.searchContainer]
export const searchBar = [stylesheet.searchBar]

export const productContainer = [stylesheet.productContainer]

export const cartIcon = [stylesheet.cartIcon, colorStyle.darkPinkBG, textStyle.textCen, colorStyle.whiteText]
export const input = [stylesheet.input]